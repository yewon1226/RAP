## < 07/22 수 >

### 1) 필드 심볼(Field Symbol)
- 메모리의 변수, 구조체, 내부테이블 등을 동적으로 참조하여 값을 읽거나 변경
```abap
FIELD-SYMBOLS <필드심볼명> TYPE 타입.

ASSIGN 변수 TO <필드심볼명>.                          "일반 변수
ASSIGN (변수명) TO <필드심볼명>.                     "동적 변수명
ASSIGN COMPONENT 필드명 OF STRUCTURE 구조체 TO <필드심볼명>.      "구조체 필드
ASSIGN COMPONENT 변수 OF STRUCTURE 구조체 TO <필드심볼명>.        "동적 구조체 필드

LOOP AT 내부테이블 ASSIGNING <필드심볼명>.
  ...
ENDLOOP.
```
```abap
CLASS zcl_a06_w03 IMPLEMENTATION.
  METHOD if_oo_adt_classrun~main.
    DATA: lv_name(20),
          lv_carrid TYPE scarr-carrid.
    FIELD-SYMBOLS <f_name> TYPE any.
    lv_name = 'LV_CARRID'.
    ASSIGN lv_name TO <f_name>.
    <f_name> = 'AA'.

    out->write( lv_carrid ).
  ENDMETHOD.
ENDCLASS.
```
</br>
</br>
</br>

### 2) 필드 심볼(Field Symbol) - 동적 변수명 ASSIGN
-  문자열에 저장된 변수명을 `ASSIGN ( )` 으로 실제 변수에 연결하여 값을 변경
```abap
FIELD-SYMBOLS <필드심볼명> TYPE any.
DATA(변수명) = '실제변수명'.
ASSIGN (변수명) TO <필드심볼명>.
<필드심볼명> = 값.
UNASSIGN <필드심볼명>.
```
```abap
CLASS zcl_a06_w03 IMPLEMENTATION.
  METHOD if_oo_adt_classrun~main.
      TYPES: BEGIN OF ts_temp,
               month(2),
               mcount TYPE i,
             END OF ts_temp.
      TYPES tt_temp TYPE TABLE OF ts_temp WITH KEY month.
      DATA lt_temp TYPE tt_temp.
  
      TYPES: BEGIN OF ts_sbook,
               month01 TYPE i,
               month02 TYPE i,
               month03 TYPE i,
               month04 TYPE i,
               month05 TYPE i,
               month06 TYPE i,
               month07 TYPE i,
               month08 TYPE i,
               month09 TYPE i,
               month10 TYPE i,
               month11 TYPE i,
               month12 TYPE i,
             END OF ts_sbook.
      TYPES tt_sbook TYPE TABLE OF ts_sbook.
      DATA lt_sbook TYPE tt_sbook.
  
      lt_temp = VALUE #(
          ( month = '01' mcount = 10 )
          ( month = '02' mcount = 0 )
          ( month = '03' mcount = 101 )
          ( month = '04' mcount = 19 )
          ( month = '05' mcount = 3 )
          ( month = '06' mcount = 7 )
          ( month = '07' mcount = 0 )
          ( month = '08' mcount = 39 )
          ( month = '09' mcount = 777 )
          ( month = '10' mcount = 34 )
          ( month = '11' mcount = 2 )
          ( month = '12' mcount = 111 )
      ).
  
      FIELD-SYMBOLS <f_month> TYPE any.
  
      LOOP AT lt_temp INTO DATA(ls_temp).
          DATA(lv_num) = ls_temp-month.
          DATA(lv_month) = 'LS_SBOOK-MONTH' && lv_num.
          ASSIGN (lv_month) TO <f_month>.
          <f_month> = ls_temp-mcount.
          CLEAR ls_temp.
          UNASSIGN <f_month>.
      ENDLOOP.
    ENDMETHOD.
ENDCLASS.
```
</br>
</br>
</br>

### 3) AT END OF (Control Break)
- 같은 값으로 묶인 데이터가 끝나는 시점(마지막 행)에서 실행되는 Control Break 문법
- `AT END OF <필드>` 가 실행되면 <필드> 뒤에 선언된 필드는 임시로 초기화(문자형은 *, 숫자형은 0)되며, `SUM` 실행 시 숫자형 필드는 그룹 합계로 변경
```abap
AT END OF <필드명>.
  ...
ENDAT.
```
```abap
CLASS zcl_a06_w04 IMPLEMENTATION.
  METHOD if_oo_adt_classrun~main.
    TYPES: BEGIN OF ts_data,
             month(2),
             mcount TYPE I,
           END OF ts_data,
           tt_data type table of ts_data.
    DATA: lt_data TYPE tt_data,
          lt_sum LIKE lt_data.
    lt_data = value #(
                        ( month = '01' mcount = '10') ( month = '02' mcount = '20') ( month = '03' mcount = '30') ( month = '04' mcount = '40')
                        ( month = '01' mcount = '20') ( month = '02' mcount = '40') ( month = '03' mcount = '30') ( month = '04' mcount = '40')
                    ).
    SORT lt_data BY month.

    LOOP AT lt_data INTO DATA(ls_data).
      AT END OF month.
        SUM.
        APPEND ls_data TO lt_sum.
      ENDAT.
    ENDLOOP.
    out->write(  lt_sum ).
  ENDMETHOD.
ENDCLASS.
```
</br>
</br>
</br>

### 4) CDS 다국어 설정(Language Association)
- Association과 언어 조건을 이용하여 로그인 언어 또는 특정 언어의 텍스트를 조회

<img width="488" height="311" alt="image" src="https://github.com/user-attachments/assets/363edfc4-b857-47b4-86f7-a1521901b606" />

</br>
</br>

```abap
association [0..1] to <텍스트 CDS> as <Association명>
  on <조건>
 and <텍스트 CDS>.Language = $session.system_language
```
```abap
@AbapCatalog.viewEnhancementCategory: [#NONE]
@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: '[A06] CDS 2'
@Metadata.ignorePropagatedAnnotations: true
define view entity ZCDS_A06_13 as select from scustom
association [0..1] to ZCDS_A06_11 as _CusttypeText
on $projection.Custtype = _CusttypeText.Custtype
and _CusttypeText.Language = $session.system_language
{
  key id as Id,
    name as Name,
    custtype as Custtype,
    _CusttypeText[1:Language = '3'].CusttypeText as TextKo, 
    _CusttypeText[1:Language = 'E'].CusttypeText as TextEn
}
```
</br>
</br>
</br>

---

</br>

## 과제 1
```abap
CLASS zcl_a06ex02 DEFINITION
  PUBLIC
  FINAL
  CREATE PUBLIC .

  PUBLIC SECTION.
    TYPES: BEGIN OF ts_temp,
             year      TYPE c LENGTH 4,
             carrid    TYPE sbook-carrid,
             class     TYPE sbook-class,
             month(2),
             cancelled TYPE sbook-cancelled,
             res       TYPE i,
             can       TYPE i,
           END OF ts_temp.
    TYPES tt_temp TYPE TABLE OF ts_temp.
    DATA: lt_temp  TYPE tt_temp,
          lt_temp2 LIKE lt_temp.

    TYPES: BEGIN OF ts_sbook,
             year   TYPE c LENGTH 4,
             carrid TYPE sbook-carrid,
             class  TYPE sbook-class,
             res01  TYPE i, can01   TYPE i,
             res02  TYPE i, can02   TYPE i,
             res03  TYPE i, can03   TYPE i,
             res04  TYPE i, can04   TYPE i,
             res05  TYPE i, can05   TYPE i,
             res06  TYPE i, can06   TYPE i,
             res07  TYPE i, can07   TYPE i,
             res08  TYPE i, can08   TYPE i,
             res09  TYPE i, can09   TYPE i,
             res10  TYPE i, can10   TYPE i,
             res11  TYPE i, can11   TYPE i,
             res12  TYPE i, can12   TYPE i,
           END OF ts_sbook.
    TYPES tt_sbook TYPE TABLE OF ts_sbook.

    METHODS set_book
      IMPORTING pa_carr TYPE sbook-carrid
      EXPORTING lt_book TYPE tt_sbook.
  PROTECTED SECTION.
  PRIVATE SECTION.
ENDCLASS.

CLASS zcl_a06ex02 IMPLEMENTATION.
  METHOD set_book.
    SELECT FROM sbook
        FIELDS substring( fldate, 1, 4 ) AS year, carrid, class, substring( fldate, 5, 2 ) AS month, cancelled
        WHERE carrid = @pa_carr
        INTO CORRESPONDING FIELDS OF TABLE @lt_temp.

    SELECT FROM sbook
        FIELDS DISTINCT substring( fldate, 1, 4 ) AS year, carrid, class
        WHERE carrid = @pa_carr
        INTO CORRESPONDING FIELDS OF TABLE @lt_book.

    SORT lt_temp BY year class month.
    SORT lt_book BY year class.

    DATA: lv_res TYPE i,
          lv_can TYPE i.

    LOOP AT lt_temp ASSIGNING FIELD-SYMBOL(<ls_temp>).
      IF <ls_temp>-cancelled = 'X'.
        <ls_temp>-can = 1.
        <ls_temp>-res = 0.
      ELSE.
        <ls_temp>-res = 1.
        <ls_temp>-can = 0.
      ENDIF.
    ENDLOOP.

    LOOP AT lt_temp INTO DATA(ls_temp).
      AT END OF month.
        SUM.
        APPEND ls_temp TO lt_temp2.
      ENDAT.
    ENDLOOP.

    FIELD-SYMBOLS: <fs_book>  TYPE any,
                   <fs_month> TYPE i.

    LOOP AT lt_temp2 INTO DATA(ls_temp2).
      READ TABLE lt_book ASSIGNING <fs_book>
        WITH KEY year   = ls_temp2-year
                 carrid = ls_temp2-carrid
                 class  = ls_temp2-class.
      IF sy-subrc = 0.
        DATA(lv_res_month) = |RES{ ls_temp2-month }|.
        ASSIGN COMPONENT lv_res_month OF STRUCTURE <fs_book> TO <fs_month>.
        <fs_month> = ls_temp2-res.

        DATA(lv_can_month) = |CAN{ ls_temp2-month }|.
        ASSIGN COMPONENT lv_can_month OF STRUCTURE <fs_book> TO <fs_month>.
        <fs_month> = ls_temp2-can.
      ENDIF.
    ENDLOOP.
  ENDMETHOD.
ENDCLASS.
```
</br>
</br>
</br>

## 과제 2
<img width="1793" height="451" alt="image" src="https://github.com/user-attachments/assets/d54c7e29-0a7f-4ce1-8260-cd41c1990df5" />
</br>
</br>

- ZCDS_A06_EX01
```abap
@AbapCatalog.viewEnhancementCategory: [#NONE]
@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'Practice 2'
@Metadata.ignorePropagatedAnnotations: true
define view entity ZCDS_A06_EX01 as select from scarr as _Scarr
association[0..1] to ZCDS_A06_EX02 as _Smeal
on _Scarr.carrid = _Smeal.Carrid
{
    key carrid as Carrid,
    carrname as Carrname,
    _Smeal.Mealnumber as MealNumber,
    _Smeal.Text as Text
} where carrid = 'AA'
```
- ZCDS_A06_EX02
```abap
@AbapCatalog.viewEnhancementCategory: [#NONE]
@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'Practice 2'
@Metadata.ignorePropagatedAnnotations: true
define view entity ZCDS_A06_EX02 as select from smeal as _Smeal
inner join smealt as _Smealt
on _Smeal.carrid = _Smealt.carrid
and _Smeal.mealnumber = _Smealt.mealnumber
and _Smealt.sprache = $session.system_language
{
    key _Smeal.carrid as Carrid,
    key _Smeal.mealnumber as Mealnumber,
    _Smealt.text as Text
}
```
